/**
 * Landing layout — deliberately bare. The landing page is a single
 * composition with its own constellation nav; no header, no footer chrome.
 */
export default function LandingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
