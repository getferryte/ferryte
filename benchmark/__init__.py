"""The Forgetting Report benchmark harness.

A realistic multi-tenant agent-memory system (`SummaryMemory`) on top of a
pluggable vector store, instrumented by Ferryte itself, so the benchmark
dogfoods the product it is reporting on.
"""
